export const sendFeedbackEmail = async (
	to: string,
	subject: string,
	body: string,
) => {
	// Placeholder: integrate with an email provider like SendGrid, SMTP, or Firebase Functions
	console.log("Sending email to", to, subject, body);
};
